DataQualityDashboard::viewDqDashboard(
  jsonPath = normalizePath("synthea-characterization.json", winslash = "/", mustWork = TRUE)
)

DataQualityDashboard::viewDqDashboard(
  jsonPath = normalizePath("synthea-convention.json", winslash = "/", mustWork = TRUE)
)

DataQualityDashboard::viewDqDashboard(
  jsonPath = normalizePath("synthea-fatal.json", winslash = "/", mustWork = TRUE)
)
